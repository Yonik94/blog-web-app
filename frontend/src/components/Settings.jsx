import React from 'react';

export const Settings = () => {
    return (
        <div className="settings" style={{textAlign: 'center'}}>
            <h1>
                Settings
            </h1>
        </div>
    );
}